//Code your kata's here :)
// use math.round();

function century(year) {
let output;
let currentYear = 0;
let Centry = currentYear[0][1] - 1;
for( currentYear; currentYear < year; currentYear++){
if(currentYear === year && year !=){
  output = currentyear[0][1]
}
}
  return output;
}